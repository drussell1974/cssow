# -*- coding: utf-8 -*-
db = DAL(configuration.get('db.uri'),
         pool_size=configuration.get('db.pool_size'),
         migrate_enabled=configuration.get('db.migrate'),
         check_reserved=['all'])


class SchemeOfWorkModel:
    _description = "Never in all their history have men been able truly to conceive of the world as one: a single sphere, a globe, having the qualities of a globe, a round earth in which all the directions eventually meet, in which there is no center because every point, or none, is center — an equal earth which all men occupy as equals. The airman's earth, if free men make it, will be truly round: a globe in practice, not in theory."

    _data = [
        { "id":"1", "name":"GCSE Computer Science", "description":_description, "key_stage":"KS4", "exam_board":"OCR", "created":"September 24, 2018", "created_by":"Dave Russell" },
        { "id":"2", "name":"KS3 ICT and Computing", "description":_description, "key_stage":"KS3", "exam_board":"", "created":"September 24, 2018", "created_by":"Dave Russell" },
        { "id":"3", "name":"A-Level Computer Science", "description":_description, "key_stage":"KS5", "exam_board":"OCR", "created":"September 24, 2018", "created_by":"Dave Russell" }
    ]

    def get_all():
        return _data


    def get(id):
        return _data[id-1]
